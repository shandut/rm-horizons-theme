{% liquid
  assign slide_blocks = section.blocks | where: 'type', 'slide'
  assign category_blocks = section.blocks | where: 'type', 'category'
  assign slide_count = slide_blocks.size
  assign autoplay_speed = section.settings.autoplay_speed | default: 5
%}

<div
  class="video-hero-slideshow section--full-width"
  id="VideoHero-{{ section.id }}"
  data-autoplay-speed="{{ autoplay_speed }}"
  data-slide-count="{{ slide_count }}"
>
  {%- comment -%} Overlay (shared across all slides) {%- endcomment -%}
  {% if section.settings.show_overlay %}
    <div
      class="video-hero-slideshow__overlay"
      style="background: linear-gradient({{ section.settings.overlay_direction }}, {{ section.settings.overlay_color }}, {{ section.settings.overlay_color | color_modify: 'alpha', 0 }});"
    ></div>
  {% endif %}

  {%- comment -%} Slides — each with its own video background {%- endcomment -%}
  {% if slide_count > 0 %}
    <div class="video-hero-slideshow__slides">
      {% for block in slide_blocks %}
        {%- liquid
          assign slide_video = block.settings.video_url
          assign slide_mobile_video = block.settings.mobile_video_url | default: slide_video
        -%}
        <div
          class="video-hero-slideshow__slide{% if forloop.first %} video-hero-slideshow__slide--active{% endif %}"
          data-slide-index="{{ forloop.index0 }}"
          {{ block.shopify_attributes }}
        >
          {%- comment -%} Per-slide video background {%- endcomment -%}
          <div class="video-hero-slideshow__media">
            {% if slide_video != blank %}
              <video
                class="video-hero-slideshow__video video-hero-slideshow__video--desktop"
                src="{{ slide_video }}"
                playsinline
                muted
                {% if forloop.first %}autoplay{% endif %}
                loop
                preload="{% if forloop.first %}auto{% else %}metadata{% endif %}"
                {% if forloop.first %}fetchpriority="high"{% endif %}
              ></video>
              {% if slide_mobile_video != slide_video %}
                <video
                  class="video-hero-slideshow__video video-hero-slideshow__video--mobile"
                  src="{{ slide_mobile_video }}"
                  playsinline
                  muted
                  {% if forloop.first %}autoplay{% endif %}
                  loop
                  preload="{% if forloop.first %}auto{% else %}metadata{% endif %}"
                  {% if forloop.first %}fetchpriority="high"{% endif %}
                ></video>
              {% endif %}
            {% else %}
              <div class="video-hero-slideshow__placeholder">
                {{ 'hero-apparel-1' | placeholder_svg_tag }}
              </div>
            {% endif %}
          </div>

          {%- comment -%} Slide text content {%- endcomment -%}
          <div class="video-hero-slideshow__slide-content">
            <div class="video-hero-slideshow__slide-inner" style="text-align: {{ block.settings.text_alignment | default: 'center' }};">
              {% if block.settings.subheading != blank %}
                <p class="video-hero-slideshow__subheading">
                  {{ block.settings.subheading }}
                </p>
              {% endif %}

              {% if block.settings.heading != blank %}
                <h2 class="video-hero-slideshow__heading">
                  {{ block.settings.heading }}
                </h2>
              {% endif %}

              <div class="video-hero-slideshow__buttons">
                {% if block.settings.button_label_1 != blank %}
                  <a
                    href="{{ block.settings.button_link_1 | default: '#' }}"
                    class="video-hero-slideshow__button video-hero-slideshow__button--{{ block.settings.button_style_1 | default: 'primary' }}"
                  >
                    {{ block.settings.button_label_1 }}
                  </a>
                {% endif %}

                {% if block.settings.button_label_2 != blank %}
                  <a
                    href="{{ block.settings.button_link_2 | default: '#' }}"
                    class="video-hero-slideshow__button video-hero-slideshow__button--{{ block.settings.button_style_2 | default: 'secondary' }}"
                  >
                    {{ block.settings.button_label_2 }}
                  </a>
                {% endif %}
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>

    {%- comment -%} Dot Navigation {%- endcomment -%}
    {% if slide_count > 1 %}
      <div class="video-hero-slideshow__dots" role="tablist" aria-label="Slide navigation">
        {% for block in slide_blocks %}
          <button
            class="video-hero-slideshow__dot{% if forloop.first %} video-hero-slideshow__dot--active{% endif %}"
            data-slide-index="{{ forloop.index0 }}"
            role="tab"
            aria-selected="{% if forloop.first %}true{% else %}false{% endif %}"
            aria-label="Go to slide {{ forloop.index }}"
          >
            <span class="video-hero-slideshow__dot-progress"></span>
          </button>
        {% endfor %}
      </div>
    {% endif %}
  {% endif %}

  {%- comment -%} Bottom Category Strip {%- endcomment -%}
  {% if category_blocks.size > 0 %}
    <div class="video-hero-slideshow__categories">
      <div class="video-hero-slideshow__categories-track">
        {% for block in category_blocks %}
          <a
            href="{{ block.settings.link | default: '#' }}"
            class="video-hero-slideshow__category"
            {{ block.shopify_attributes }}
          >
            {% if block.settings.image != blank %}
              <div class="video-hero-slideshow__category-image">
                {{
                  block.settings.image
                  | image_url: width: 200
                  | image_tag: widths: '100, 150, 200', sizes: '100px', loading: 'lazy'
                }}
              </div>
            {% else %}
              <div class="video-hero-slideshow__category-image video-hero-slideshow__category-image--placeholder">
                {{ 'collection-1' | placeholder_svg_tag }}
              </div>
            {% endif %}
            {% if block.settings.label != blank %}
              <span class="video-hero-slideshow__category-label">
                {{ block.settings.label }}
              </span>
            {% endif %}
          </a>
        {% endfor %}
      </div>
    </div>
  {% endif %}
</div>

<script>
  (function() {
    const heroEl = document.getElementById('VideoHero-{{ section.id }}');
    if (!heroEl) return;

    const speed = parseInt(heroEl.dataset.autoplaySpeed, 10) * 1000;
    const slideCount = parseInt(heroEl.dataset.slideCount, 10);
    if (slideCount <= 1) return;

    const slides = heroEl.querySelectorAll('.video-hero-slideshow__slide');
    const dots = heroEl.querySelectorAll('.video-hero-slideshow__dot');
    let current = 0;
    let timer = null;
    let isPaused = false;

    function pauseSlideVideos(slide) {
      slide.querySelectorAll('video').forEach(function(v) { v.pause(); });
    }

    function playSlideVideos(slide) {
      slide.querySelectorAll('video').forEach(function(v) {
        v.currentTime = 0;
        v.play().catch(function() {});
      });
    }

    function goToSlide(index) {
      pauseSlideVideos(slides[current]);
      slides[current].classList.remove('video-hero-slideshow__slide--active');
      dots[current].classList.remove('video-hero-slideshow__dot--active');
      dots[current].setAttribute('aria-selected', 'false');

      current = index;

      slides[current].classList.add('video-hero-slideshow__slide--active');
      dots[current].classList.add('video-hero-slideshow__dot--active');
      dots[current].setAttribute('aria-selected', 'true');
      playSlideVideos(slides[current]);
    }

    function nextSlide() {
      goToSlide((current + 1) % slideCount);
    }

    function startAutoplay() {
      stopAutoplay();
      timer = setInterval(nextSlide, speed);
    }

    function stopAutoplay() {
      if (timer) {
        clearInterval(timer);
        timer = null;
      }
    }

    dots.forEach(function(dot) {
      dot.addEventListener('click', function() {
        var index = parseInt(this.dataset.slideIndex, 10);
        goToSlide(index);
        startAutoplay();
      });
    });

    heroEl.addEventListener('mouseenter', function() {
      isPaused = true;
      stopAutoplay();
    });

    heroEl.addEventListener('mouseleave', function() {
      isPaused = false;
      startAutoplay();
    });

    // Respect reduced motion
    var prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
    if (!prefersReducedMotion.matches) {
      startAutoplay();
    }
    prefersReducedMotion.addEventListener('change', function(e) {
      if (e.matches) {
        stopAutoplay();
      } else if (!isPaused) {
        startAutoplay();
      }
    });
  })();
</script>

{% stylesheet %}
  .video-hero-slideshow {
    position: relative;
    width: 100%;
    height: 100svh;
    min-height: 500px;
    max-height: 1000px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  /* Overlay (shared, sits above all slides) */
  .video-hero-slideshow__overlay {
    position: absolute;
    inset: 0;
    z-index: 2;
    pointer-events: none;
  }

  /* Slides container */
  .video-hero-slideshow__slides {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  /* Individual slide — full bleed, stacked */
  .video-hero-slideshow__slide {
    position: absolute;
    inset: 0;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.8s ease, visibility 0.8s ease;
    pointer-events: none;
  }

  .video-hero-slideshow__slide--active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    z-index: 1;
  }

  /* Per-slide video background */
  .video-hero-slideshow__media {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .video-hero-slideshow__video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .video-hero-slideshow__video--mobile {
    display: none;
  }

  @media screen and (max-width: 749px) {
    .video-hero-slideshow__video--desktop {
      display: none;
    }
    .video-hero-slideshow__video--mobile {
      display: block;
    }
  }

  .video-hero-slideshow__placeholder {
    width: 100%;
    height: 100%;
    background: var(--color-background);
  }

  .video-hero-slideshow__placeholder svg {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Slide text content — positioned over the video */
  .video-hero-slideshow__slide-content {
    position: relative;
    z-index: 1;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: center;
    padding: 0 clamp(1.5rem, 4vw, 4rem);
    padding-bottom: clamp(2rem, 6vw, 5rem);
  }

  .video-hero-slideshow__slide-inner {
    width: 100%;
    max-width: 700px;
    margin: 0 auto;
  }

  /* Typography */
  .video-hero-slideshow__subheading {
    font-family: var(--font-body--family);
    font-size: clamp(0.75rem, 1.2vw, 1rem);
    font-weight: 400;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.85);
    margin: 0 0 0.75rem;
    line-height: 1.3;
  }

  .video-hero-slideshow__heading {
    font-family: var(--font-heading--family);
    font-size: clamp(2rem, 5.5vw, 4.5rem);
    font-weight: var(--font-heading--weight, 400);
    line-height: 1.05;
    color: #fff;
    margin: 0 0 1.5rem;
    text-transform: none;
    letter-spacing: -0.01em;
  }

  /* Buttons */
  .video-hero-slideshow__buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
  }

  .video-hero-slideshow__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.875rem 2rem;
    font-family: var(--font-body--family);
    font-size: clamp(0.75rem, 1vw, 0.875rem);
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-decoration: none;
    transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
    cursor: pointer;
    line-height: 1;
    white-space: nowrap;
  }

  .video-hero-slideshow__button--primary {
    background-color: #fff;
    color: #1a1a1a;
    border: 1px solid #fff;
  }

  .video-hero-slideshow__button--primary:hover {
    background-color: transparent;
    color: #fff;
  }

  .video-hero-slideshow__button--secondary {
    background-color: transparent;
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.6);
  }

  .video-hero-slideshow__button--secondary:hover {
    background-color: #fff;
    color: #1a1a1a;
    border-color: #fff;
  }

  /* Dot Navigation */
  .video-hero-slideshow__dots {
    position: absolute;
    bottom: clamp(2rem, 6vw, 5rem);
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
    display: flex;
    justify-content: center;
    gap: 8px;
  }

  .video-hero-slideshow__dot {
    width: 40px;
    height: 3px;
    background: rgba(255, 255, 255, 0.35);
    border: none;
    padding: 0;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    border-radius: 2px;
    transition: background 0.3s ease;
  }

  .video-hero-slideshow__dot:hover {
    background: rgba(255, 255, 255, 0.55);
  }

  .video-hero-slideshow__dot-progress {
    position: absolute;
    top: 0;
    left: 0;
    width: 0;
    height: 100%;
    background: #fff;
    border-radius: 2px;
  }

  .video-hero-slideshow__dot--active {
    background: rgba(255, 255, 255, 0.35);
  }

  .video-hero-slideshow__dot--active .video-hero-slideshow__dot-progress {
    animation: dotProgress var(--autoplay-speed, 5s) linear forwards;
  }

  @keyframes dotProgress {
    from { width: 0; }
    to { width: 100%; }
  }

  /* Category Strip */
  .video-hero-slideshow__categories {
    position: relative;
    z-index: 3;
    background: rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    padding: 0.75rem 0;
  }

  .video-hero-slideshow__categories-track {
    display: flex;
    justify-content: center;
    gap: clamp(1rem, 3vw, 2.5rem);
    padding: 0 1rem;
    overflow-x: auto;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .video-hero-slideshow__categories-track::-webkit-scrollbar {
    display: none;
  }

  .video-hero-slideshow__category {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
    text-decoration: none;
    flex-shrink: 0;
    transition: opacity 0.3s ease;
  }

  .video-hero-slideshow__category:hover {
    opacity: 0.75;
  }

  .video-hero-slideshow__category-image {
    width: clamp(48px, 6vw, 72px);
    height: clamp(48px, 6vw, 72px);
    border-radius: 50%;
    overflow: hidden;
    border: 2px solid rgba(255, 255, 255, 0.3);
  }

  .video-hero-slideshow__category-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .video-hero-slideshow__category-image--placeholder {
    background: rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .video-hero-slideshow__category-image--placeholder svg {
    width: 60%;
    height: 60%;
  }

  .video-hero-slideshow__category-label {
    font-family: var(--font-body--family);
    font-size: clamp(0.6rem, 0.9vw, 0.75rem);
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #fff;
    text-align: center;
    white-space: nowrap;
  }

  /* Mobile Adjustments */
  @media screen and (max-width: 749px) {
    .video-hero-slideshow {
      height: 85svh;
      min-height: 450px;
      max-height: 800px;
    }

    .video-hero-slideshow__slide-content {
      padding: 0 1.25rem;
      padding-bottom: 1.5rem;
    }

    .video-hero-slideshow__heading {
      font-size: clamp(1.75rem, 8vw, 2.75rem);
      margin-bottom: 1.25rem;
    }

    .video-hero-slideshow__button {
      padding: 0.75rem 1.5rem;
      font-size: 0.7rem;
    }

    .video-hero-slideshow__dot {
      width: 28px;
    }

    .video-hero-slideshow__dots {
      bottom: 1.5rem;
    }

    .video-hero-slideshow__categories {
      padding: 0.5rem 0;
    }

    .video-hero-slideshow__categories-track {
      justify-content: flex-start;
      gap: 1rem;
      padding: 0 1.25rem;
    }
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .video-hero-slideshow__slide {
      transition: none;
    }

    .video-hero-slideshow__dot-progress {
      animation: none;
    }

    .video-hero-slideshow__dot--active .video-hero-slideshow__dot-progress {
      width: 100%;
    }

    .video-hero-slideshow__video {
      animation: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Video hero slideshow",
  "tag": "section",
  "class": "video-hero-section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Overlay"
    },
    {
      "type": "checkbox",
      "id": "show_overlay",
      "label": "Show overlay",
      "default": true
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay color",
      "default": "#000000"
    },
    {
      "type": "select",
      "id": "overlay_direction",
      "label": "Gradient direction",
      "options": [
        { "value": "to top", "label": "Bottom to top" },
        { "value": "to bottom", "label": "Top to bottom" },
        { "value": "to top left", "label": "Bottom-right to top-left" }
      ],
      "default": "to top"
    },
    {
      "type": "header",
      "content": "Autoplay"
    },
    {
      "type": "range",
      "id": "autoplay_speed",
      "label": "Seconds per slide",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "default": 5
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "limit": 6,
      "settings": [
        {
          "type": "header",
          "content": "Video"
        },
        {
          "type": "text",
          "id": "video_url",
          "label": "Video URL",
          "info": "Paste the full Cloudinary or external video URL for this slide."
        },
        {
          "type": "text",
          "id": "mobile_video_url",
          "label": "Mobile video URL",
          "info": "Optional: different video for mobile. Leave blank to use the desktop video."
        },
        {
          "type": "header",
          "content": "Content"
        },
        {
          "type": "text",
          "id": "subheading",
          "label": "Subheading",
          "default": "Our pioneering original"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "The Heritage Collection"
        },
        {
          "type": "select",
          "id": "text_alignment",
          "label": "Text alignment",
          "options": [
            { "value": "center", "label": "Center" },
            { "value": "left", "label": "Left" }
          ],
          "default": "center"
        },
        {
          "type": "header",
          "content": "Button 1"
        },
        {
          "type": "text",
          "id": "button_label_1",
          "label": "Button label",
          "default": "Shop the collection"
        },
        {
          "type": "url",
          "id": "button_link_1",
          "label": "Button link"
        },
        {
          "type": "select",
          "id": "button_style_1",
          "label": "Button style",
          "options": [
            { "value": "primary", "label": "Primary (solid)" },
            { "value": "secondary", "label": "Secondary (outline)" }
          ],
          "default": "primary"
        },
        {
          "type": "header",
          "content": "Button 2"
        },
        {
          "type": "text",
          "id": "button_label_2",
          "label": "Button label"
        },
        {
          "type": "url",
          "id": "button_link_2",
          "label": "Button link"
        },
        {
          "type": "select",
          "id": "button_style_2",
          "label": "Button style",
          "options": [
            { "value": "primary", "label": "Primary (solid)" },
            { "value": "secondary", "label": "Secondary (outline)" }
          ],
          "default": "secondary"
        }
      ]
    },
    {
      "type": "category",
      "name": "Category link",
      "limit": 10,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "New"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Video hero slideshow",
      "category": "Banners",
      "blocks": [
        {
          "type": "slide",
          "settings": {
            "subheading": "Our pioneering original",
            "heading": "The Heritage Collection",
            "button_label_1": "Shop the collection",
            "button_style_1": "primary"
          }
        },
        {
          "type": "slide",
          "settings": {
            "subheading": "Timeless vintage meets the big sky",
            "heading": "The Drover",
            "button_label_1": "Shop men's & kids",
            "button_style_1": "primary",
            "button_label_2": "Accessories & more",
            "button_style_2": "secondary"
          }
        },
        {
          "type": "slide",
          "settings": {
            "subheading": "Redefining modern essentials",
            "heading": "The New Collection",
            "button_label_1": "Discover now",
            "button_style_1": "primary"
          }
        },
        {
          "type": "category",
          "settings": {
            "label": "New"
          }
        },
        {
          "type": "category",
          "settings": {
            "label": "Jackets"
          }
        },
        {
          "type": "category",
          "settings": {
            "label": "Shirts"
          }
        },
        {
          "type": "category",
          "settings": {
            "label": "Knitwear"
          }
        },
        {
          "type": "category",
          "settings": {
            "label": "T-Shirts"
          }
        }
      ]
    }
  ]
}
{% endschema %}
